[project]
name = "email-ai-env"
version = "0.1.0"
description = "OpenEnv Email AI Environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "pydantic",
    "openenv-core>=0.2.0"
]